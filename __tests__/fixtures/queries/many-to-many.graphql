query {
  a: allPeople {
    nodes {
      personName
      teamsByTeamMemberPersonIdAndTeamId {
        # FIXME: SQL for resolving edge columns isn't working yet
        #edges {
        #  node {
        #    teamName
        #  }
        #  createdAt
        #}
        nodes {
          teamName
        }
      }
    }
  }
}
