query {
  a: allPeople {
    nodes {
      __typename
      personName
      # TODO: Get interface level many-to-many working with polymorphic juntions
      # Ensure that we can access the polymorphic join table from the person interface
      # teamsList {
      #   id
      # }
      ... on InternalEmployee {
        department
      }
      ... on ExternalPerson {
        companyName
      }
    }
  }
}
